export const metadata = {
  title: 'LightLink ZK Proof Service',
  description: 'ZK Proof Generation Service for LightLink Oracle',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
} 